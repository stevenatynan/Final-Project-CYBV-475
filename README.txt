-If required packages from requirements.txt are not already installed, install packages in Command Prompt using:
pip install -r requirements.txt
-Run tune_model.py first to fine-tune the HuggingFace model
-Run chatbot.py to start the local web server and navigate to the URL to use the chatbot